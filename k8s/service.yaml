apiVersion: v1
kind: Service
metadata:
  name: middleware-integration-service
spec:
  selector:
    app: middleware-integration-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
